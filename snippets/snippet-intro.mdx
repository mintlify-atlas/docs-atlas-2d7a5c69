Pulse is a logging and network debugging SDK for Apple platforms. It captures `URLSession` traffic, stores logs persistently on-device, and provides a native SwiftUI console you can embed directly in your app.
